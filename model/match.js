// match skal laves som en funktion? 
// sammenlign interest: sexuality + hobbies og user: køn 
// når man er hetro: forkselligt køn + samme seksualitet + mindste en fælles hobby = match
// når man er bi= samme ELLER forskellig køn + samme seksuallitet + mindst en fælles hobby = match
// når man er homo = samme køn + samme seksualitet + mindste en fælles hobby = match



// compare function() ... ? 
// regex? 

function match(){
   // hvad skal funkyionen returnere ? hvordan får jeg den til at angive hvilken/ hvilke users den matcher med? skal man så kunne se deres data? 
    // kan jeg få den til at vise mig billederne af deres matces??? 
}

//var match= User.match(gender && sexuality && hobbies);
//console.log(match); 
